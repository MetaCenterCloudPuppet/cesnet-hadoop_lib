fixtures:
  repositories:
    stdlib:
      repo: "https://github.com/puppetlabs/puppetlabs-stdlib"
      # for future parser tests with puppet < 4
      ref: "4.25.1"
    alternatives:
      repo: "https://github.com/voxpupuli/puppet-alternatives"
      # for tests with puppet < 4
      ref: "v1.0.1"
  symlinks:
    hadoop_lib: "#{source_dir}"
